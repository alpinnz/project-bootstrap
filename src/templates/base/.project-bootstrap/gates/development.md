# Development Gate

A change must meet all of the following before it is considered done:

- behavior is implemented
- verification was executed
- the diff was reviewed

## Verification

Run the applicable checks: format, lint, type/static checks, build, unit tests, integration tests, E2E tests. If verification cannot be performed, state that explicitly.
