# Spec Kit Integration

This project uses specification-driven development.

- Features are specified before implementation.
- A feature specification captures the requirement, contract, edge cases,
  and acceptance criteria before code is written.
- Implementation and tests trace back to the specification.
- The repository (not an external tool) remains the source of truth.

Place feature specifications under `specs/`.
